Put audio files here and list them in `playlist.json`.

Example:

```json
[
  { "title": "first song", "file": "first-song.mp3", "note": "optional note" },
  { "title": "second song", "file": "second-song.ogg" }
]
```

The files must be committed with the site for GitHub Pages to serve them.
